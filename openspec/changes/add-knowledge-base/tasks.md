## 1. 笔记地基（note-storage）

- [ ] 1.1 初始化笔记库结构：创建 `notes/` 目录与 `.gitignore`，提交初始 git 提交；验证 `git log` 含初始提交且 `.gitignore` 生效
- [ ] 1.2 确定笔记书写约定（Markdown 文件名规范、目录组织），创建一篇示例学习笔记；验证该笔记是纯文本、任意文本编辑器可读
- [ ] 1.3 用 Obsidian 打开 `notes/` 目录；验证笔记可编辑、保存后仍为纯文本 Markdown、迁移到其他目录内容不丢失

## 2. 语义检索（semantic-search）

- [ ] 2.1 初始化 Python 项目结构（`pyproject.toml` 或 `requirements.txt`）并安装依赖；验证 `python -c "import <核心依赖>"` 成功
- [ ] 2.2 实现模型抽象层：OpenAI 兼容的 embedding 接口，可通过配置指定 `base_url` 与模型名；验证能调用云端 API 返回向量
- [ ] 2.3 实现变更检测：用 git（status/diff）识别 `notes/` 下新增、修改、删除的 Markdown 文件；验证返回的改动文件列表与 `git status` 一致
- [ ] 2.4 实现增量索引：仅对变更文件生成向量并写入本地向量存储；验证索引文件生成，且未变更文件不触发 API 调用
- [ ] 2.5 实现语义搜索命令：输入自然语言查询，返回 top-k 语义相关笔记列表；验证返回结果语义相关（不要求关键词精确匹配）
- [ ] 2.6 实现索引缓存状态：记录已索引文件，重复运行跳过未变更文件；验证连续运行两次，第二次不重复调用 embedding API

## 3. 关联发现（note-association）

- [ ] 3.1 实现新笔记入库时的关联计算：新向量与既有向量做相似度计算，返回最相关的旧笔记；验证新增笔记后返回相关旧笔记列表
- [ ] 3.2 实现修改后重新关联：笔记内容变更后基于新内容重算关联；验证关联结果随内容更新
- [ ] 3.3 输出关联结果：将关联提示以可查看的形式呈现（如写入笔记元信息或生成关联报告）；验证用户能读到"相关内容"提示
