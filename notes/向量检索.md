# 向量检索（Vector Search）

## 是什么

把文本转成高维向量，用向量之间的相似度（如余弦相似度）来查找语义相近的内容，而不依赖关键词精确匹配。

## 核心概念

- **Embedding**：把文本映射到向量空间的模型
- **相似度**：余弦相似度、点积、欧氏距离
- **向量索引**：加速近邻搜索的结构（如 HNSW）

## 与传统关键词检索的区别

传统检索靠精确关键词匹配；向量检索靠语义，能召回"说法不同、意思相同"的内容。

## 关联

- 语义搜索
- 增量索引
